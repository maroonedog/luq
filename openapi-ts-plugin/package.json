{
  "name": "@maroonedog/openapi-ts-luq",
  "version": "0.1.0",
  "description": "Generate Luq validators from OpenAPI. A plugin for @hey-api/openapi-ts, and the generator it wraps.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/maroonedog/luq.git",
    "directory": "openapi-ts-plugin"
  },
  "type": "module",
  "files": ["dist"],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "jest --no-watch --no-watchman -c jest.config.cjs",
    "verify": "npm run typecheck && npm test"
  },
  "peerDependencies": {
    "@maroonedog/luq": ">=2.0.0"
  }
}
