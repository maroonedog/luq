{
  "$schema": "./node_modules/oxlint/configuration_schema.json",
  "plugins": ["typescript", "unicorn"],
  "env": { "es2022": true, "node": true },
  "categories": { "correctness": "error" },
  "rules": {
    "max-lines": ["error", { "max": 200, "skipBlankLines": false, "skipComments": false }],
    "max-classes-per-file": ["error", 1],
    "prefer-const": "error",
    "no-var": "error",
    "eqeqeq": ["error", "smart"],
    "no-console": ["error", { "allow": ["warn", "error"] }],
    "typescript/no-explicit-any": "error",
    "typescript/no-non-null-assertion": "error",
    "typescript/no-unused-vars": ["error", { "argsIgnorePattern": "^_", "varsIgnorePattern": "^_" }],
    "typescript/consistent-type-assertions": [
      "error",
      { "assertionStyle": "as", "objectLiteralTypeAssertions": "never" }
    ],
    "typescript/no-empty-object-type": "error",
    "typescript/no-unsafe-function-type": "error",
    "typescript/no-wrapper-object-types": "error",

    // In Draft-07 if/then/else, then is a keyword, not a Promise.
    // This unicorn rule mistakes the former for the latter, so it is turned off.
    "unicorn/no-thenable": "off"
  },
  "overrides": [
    {
      // A generated file's length is the plugin count, not a judgement anyone
      // made, and the generator is held to the 200-line limit like every other
      // source file. Capping the OUTPUT would only push it into emitting
      // denser lines, which is the opposite of what the limit is for.
      "files": ["**/*.generated.ts"],
      "rules": { "max-lines": "off" }
    },
    {
      "files": ["**/*.test.ts", "test/**/*.ts"],
      "rules": {
        "max-lines": "off",
        "typescript/no-explicit-any": "off",
        "typescript/no-non-null-assertion": "off"
      }
    }
  ]
}
