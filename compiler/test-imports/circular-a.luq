// circular-a.luq - circular dependency test A

import { ServiceB } from "./circular-b.luq";

/// ServiceA - a service for testing circular dependencies
/// This is a Dart-style documentation comment
/// Multi-line comments are supported too
export interface ServiceA {
    @
    id: string;
    
    
    relatedB: ServiceB;
}


/// A function that processes a ServiceA
/// It works together with ServiceB
export function processA(): void {
    /// The part that uses ServiceB
    
}

@validator
function validateUser(payload: string) {
    return true;
}