// Test file for LSP functionality with logos+chumsky parser

@validator
@required
function validateUser(user: { name: string, age: number }) {
    if (!user.name) {
        return "Name is required";
    }
    
    if (user.age < 18) {
        return "Must be 18 or older";
    }
    
    return true;
}

type UserProfile = {
    id: string;
    name: string;
    email: string;
    age: number;
    isActive: boolean;
};

interface AuthService {
    login(username: string, password: string): Promise<boolean>;
    logout(): void;
    getCurrentUser(): UserProfile | null;
}

// Test various token types
const testString = "Hello, world!";
const testNumber = 42;
const testBoolean = true;
const testNull = null;
const testUndefined = undefined;
const testRegex = /[a-z]+/gi;

// Test template literals (if supported)
const greeting = `Hello, ${testString}!`;

// Test JSDoc comments
/**
 * Calculate the sum of two numbers
 * @param a First number
 * @param b Second number
 * @returns The sum of a and b
 */
function add(a: number, b: number): number {
    return a + b;
}

export { validateUser, UserProfile, AuthService };