// Test file for completion debugging
// Try typing @ on lines 9 and 13 and press Ctrl+Space

export interface TestInterface {
    // Type @ here and press Ctrl+Space
    // Expected: Only "validator" should appear
    // Problem: Shows "loading..." and hangs after repeated attempts
    
    @
    field1: string;
    
    // Try again here
    @
    field2: number;
}

// Define a validator to test completion
@validator
function validateTest(payload: any) {
    return true;
}